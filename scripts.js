// Espera a que todo el contenido del DOM esté completamente cargado antes de ejecutar el código
document.addEventListener('DOMContentLoaded', () => {
  
  // Muestra un mensaje en la consola del navegador indicando que la aplicación ha sido cargada
  console.log("Movesafe app loaded.");
});

